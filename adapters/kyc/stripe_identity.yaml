name: stripe_identity
module: kyc
version: 0.1.0
description: Stripe Identity verification adapter
languages: [typescript, python, go, rust, java]
implements:
  - createVerificationSession
  - getVerificationResult
does_not_implement: []
config:
  required:
    - name: api_key
      type: string
      description: Stripe secret key
      secret: true
