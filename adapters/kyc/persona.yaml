name: persona
module: kyc
version: 0.1.0
description: Persona identity verification adapter
languages: [typescript, python, go, rust, java]
implements:
  - createInquiry
  - getInquiryStatus
  - getVerificationReport
does_not_implement: []
config:
  required:
    - name: api_key
      type: string
      description: Persona API key
      secret: true
